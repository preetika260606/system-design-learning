###What is HTTP?

->
Hyper Text Transfer Protocol (HTTPS) is the standard Communication protocol used between a Client (frontend) and Server (backend).

->
  The client sends a request to the server.
  The server process the request and sends back a response.

->
Client (Browser)
       |
       | HTTP Request
       v
Server
       |
       | HTTP Response
       v
Client

